spring:
  datasource:
    url: jdbc:mysql://mysql:3307/springdb
    username: root
    password: root
    
#datasource specific properties
  jpa:    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:      ddl-auto: update
 #change port number
server:  port: 8081     
      